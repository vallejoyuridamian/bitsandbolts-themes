# Bits and Bolts Repository Context

The authoritative workspace files are in the parent workspace directory.
Before consequential work, load `../GLOBAL_CONTEXT.md`
and `../WORKSPACE_DICTIONARY.md`, then follow the global
context router for project hot files and one collaboration mode. After context
compression, reload those files within the global context budget before acting.
A compressed summary does not replace them. Acknowledge dictionary triggers
in commentary before taking their required action.
